# Steg 2: Att förändra

- Flytta all fraktal dokumentation från `src/features/[domän]/doc/` till `doc/features/[domän]/`.
- Skapa struktur för femstegsprocessen i `doc/LAST_CYCLE/`.
- Ta bort föråldrade `doc/`-undermappar från `src/features/` så att `src/` blir helt ren.
