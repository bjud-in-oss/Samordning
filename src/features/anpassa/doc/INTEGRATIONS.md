# Integrationer: anpassa

1. **CSS Integration (`src/index.css`)**: Kopplar temavalet (`data-theme`) till Tailwind- och CSS-variabler för knappar, modaler, bakgrunder och flöden.
2. **App-integration**: Exporterar `OnboardingWizard` och relaterade typer via `src/features/anpassa/index.ts` till `src/App.tsx`.
3. **Inga externa nätverks- eller AI-beroenden**: Domänen hanterar inställningar lokalt på klientsidan.
