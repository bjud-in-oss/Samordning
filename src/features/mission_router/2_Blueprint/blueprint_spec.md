UI-state (currentView) får ALDRIG bindas till auto-save-events (onSave) i komponenter med reaktiva useEffects, eftersom det skapar bounce-back-loopar. Separera explicit stängning (onClose) från datasynk (onSave).
